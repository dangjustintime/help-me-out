<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Help Me Out</title>
    </head>
    <body>
        <h1>Jobs</h1>
        <ul>
            <% for (let i = 0; i < Jobs.length; i++) { %>
            <li><a href="#"><%= Jobs[i].title %></a></li>
            <% } %>
        </ul>
    </body>
</html>
