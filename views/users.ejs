<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Help Me Out</title>
    </head>
    <body>
        <h1>Users</h1>
        <ul>
            <% for (let i = 0; i < Users.length; i++) { %>
            <li>
                <a href="/user/<%= Users[i].id %>"><%= Users[i].name %></a>
            </li>
            <% } %> 
        </ul>
    </body>
</html>
